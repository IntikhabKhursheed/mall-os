{
  "installCommand": "npm install",
  "buildCommand": "npx ng build --configuration=production",
  "outputDirectory": "dist/mallos-client/browser",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
